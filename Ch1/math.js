// We create four arrow functions
const add = (a,b) => a + b;
const sub = (a,b) => a - b;
const multiply = (a,b) =>  a * b;
const divide = (a,b) => a/b;

// We need to export the functions
module.exports = {add,sub,multiply,divide}